# Round 01 — 2026-06-25

- 验证日期：2026-06-25（Asia/Shanghai）
- 分支：`codex/goal-release-evidence`
- 基线：`origin/codex/core-product-integration` @ `fb4c3a7041c4821ce8a69e05c51058805b822785`
- 执行环境：macOS 26.5.1（Darwin 25.5.0 arm64），Xcode 26.5，rustc 1.96.0 stable，
  `OHOS_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk/default`
- 工作目录：throwaway worktree `/tmp/release-evidence-wt`（避免与共享主工作树
  `codex/harmony-napi-integration` 上的并发 agent 互相干扰）
- 约束：仅运行只读验证命令，不改实现；smoke 通过不记为 App/device 通过。

## 命令逐条记录

每条含：命令 / 平台 / 结果 / 失败原因 / 是否可作为 release gate。

### 1. `cargo fmt --check`
- 平台：macOS host
- 结果：PASS（exit 0）
- 失败原因：无
- release gate：是（格式 gate）

### 2. `cargo test --workspace`
- 平台：macOS host
- 结果：PASS。92 passed / 0 failed。明细：
  - reader_cli unittests 1、fixture_vertical 1
  - reader_content 10、reader_contract 12、reader_core 5、reader_js 16
  - reader_rule（rule_edgecases 8 + rule_engine 7 = 15）
  - reader_runtime 28、reader_storage 4
  - reader_domain / reader_local_book / reader_rss / reader_sync 0
  - doc-tests 全 0
- 失败原因：无
- release gate：是（全 workspace 测试 gate）
- 注：本基线 reader-rule/reader-js 测试数较少；`codex/rule-engine-parity` 分支的 51+51
  不属本基线，不计入。

### 3. `cargo run -p reader-cli -- --conformance`
- 平台：macOS host
- 结果：PASS。18/18 conformance cases passed，0 failed。覆盖命令校验、配置校验、
  host.request/complete/error、cancel 幂等。
- 失败原因：无
- release gate：是（协议 conformance gate）

### 4. `cargo run -p reader-cli -- --fixture-vertical tests/fixtures/remote_source/basic_source.json`
- 平台：macOS host
- 结果：PASS。9 步全通：
  - 1 source.import → 2 book.search(inline) → 3 book.search(host.http.execute)+host.complete
    → 5 book.detail → 6 book.toc → 7 chapter.content(rule) → 8 reading.progress.update
    → 9 chapter.content(JS unsupported)：正确返回结构化 error
    `JS rule unsupported in V1: host callback java.get failed: unregistered host callback: java.get`
    （`unsupported:true`、`retryable:true`）
- 失败原因：无（requestId 9 的 error 是预期行为，非失败）
- release gate：是（Core-side remote.reading.v1 垂直 smoke gate）

### 5. `cargo run -p reader-cli`（默认 `--info`）
- 平台：macOS host
- 结果：PASS。返回 `abiVersion=1`、`buildVersion=reader-core-native 0.1.0`、
  `protocolVersion=1`、capabilities 9 项（core.info / runtime.ping / runtime.hostSmoke /
  host.complete / host.error / host.bus.v1 / http.execute / runtime.config.v1 / remote.reading.v1）
- 失败原因：无
- release gate：是（ABI smoke gate）

### 6. `./scripts/ffi-smoke.sh`
- 平台：macOS host
- 结果：PASS。`cargo build -p reader-ffi --release` 成功；C 二进制 `target/ffi-smoke-c`
  与 C++ 二进制 `target/ffi-smoke-cxx` 均链接 `libreader_core.a` 并输出
  `{"type":"result",...,"pong":true}`。
- 失败原因：无
- release gate：是（host FFI ABI smoke gate）
- 注：**host FFI smoke，非 App/device。** 链接器告警见下“非致命告警”。

### 7. `./scripts/check-ios-swift-wrapper.sh`
- 平台：iOS（xcframework：aarch64-apple-ios + aarch64-apple-ios-sim）+ macOS host sim
- 结果：PASS。内部依次：
  - `build-ios-xcframework.sh`：构建 device+sim release 静态库，生成
    `target/ios/ReaderCore.xcframework`，headers/module.modulemap 存在，sim swift typecheck ok。
  - `cargo build -p reader-ffi`（debug）。
  - 编译并运行临时 Swift smoke 二进制（链接 debug `libreader_core.a`）：`swift client smoke passed`
    （校验 abiVersion、core.info capabilities 含 runtime.ping、runtime.ping pong=true）。
- 失败原因：无
- release gate：是（Core-side iOS 产物 + Swift wrapper 编译/链接/host sim runtime smoke gate）；
  **否** for iOS App/device（未构建 App、未模拟器/真机启动）
- 注：**host sim wrapper smoke，非 iOS App/device。**

### 8. `./scripts/build-harmony-napi.sh`
- 平台：HarmonyOS/OHOS（aarch64-unknown-linux-ohos）
- 结果：PASS。内部依次：
  - `build-ohos.sh`：`cargo rustc -p reader-ffi --release --target aarch64-unknown-linux-ohos --lib --crate-type staticlib`，
    生成 `target/aarch64-unknown-linux-ohos/release/libreader_core.a`。
  - CMake/Ninja（SDK 工具链）构建 `bindings/harmony/native`，生成
    `target/harmony-napi/arm64-v8a/libreader_core_napi.so`。
- 失败原因：无
- release gate：是（Core-side OHOS 静态库 + NAPI `.so` 构建 gate）；
  **否** for HAP/device（未打 HAP、未真机/DevEco 启动）
- 注：**Core-side NAPI `.so` 构建 smoke，非 HAP/device。**

## 非致命告警（记录，不阻塞）

- `ffi-smoke.sh` 与 `check-ios-swift-wrapper.sh` 链接时：
  `ld: warning: object file (...libregexp.o/libunicode.o/cutils.o/quickjs.o/libbf.o) was built
  for newer 'macOS' version (26.5) than being linked (26.0)`。
  原因：QuickJS C 对象编译目标 macOS 版本高于链接目标。仅告警，产物与 smoke 均通过。
- `build-harmony-napi.sh` CMake 阶段：
  `clang++: warning: argument unused during compilation: '--gcc-toolchain=...'`。仅告警。

## 汇总

| gate 类别 | 命令数 | 通过 | 失败 |
|-----------|--------|------|------|
| Core-side smoke gate（1–8） | 8 | 8 | 0 |
| App/device gate | 0（均未验证） | — | — |

- **Core-side smoke release gate：满足。**
- **App/device release gate：未满足**（iOS App、HarmonyOS HAP、Android 均未验证）。
- **Android**：无构建路径，不可声明（见 [blockers.md](../blockers.md) B4）。

## 未跑项（见 [unverified.md](../unverified.md)）

- `./scripts/build-local.sh` 整脚本端到端（分量已覆盖）。
- `./scripts/integration-queue.sh`（人工工具）。
- iOS App / HarmonyOS HAP / Android 任何 App/device 验证。
- 真实网络/TLS 书源端到端。
