# Smoke Round

Synthetic placeholder markdown for the evidence-indexer sample tree.

This file lives under `samples/tooling/evidence-index/reports/` so the
indexer's markdown classifier can be exercised on a real (placeholder)
`.md` file. The filename keyword `smoke` tags this entry as tier `smoke`.
