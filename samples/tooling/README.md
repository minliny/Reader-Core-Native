# Dev-time tooling samples

Synthetic, sanitized sample inputs for the tools under `tools/`. Each
tool owns a subdirectory here (e.g. `evidence/`, `corpus-batches/`).
All content is fabricated with `example.test` hosts and placeholder
values — no real tokens, accounts, or copyrighted text.
